<div class="card">
    <div class="card-header bg-dark text-light">Level 3</div>
    <div class="card-body">
        <p>Using the SportsORM, complete all the following queries and show their results on Level3.cshtml.</p>
        <ol>
            <li>...all teams, past and present, that Samuel Evans has played with</li>
            <table class="table table-striped col-10">
                <tr>
                  <th>Player</th>
                  <th>Team</th>
              </tr>
              @foreach (PlayerTeam item in ViewBag.Evans.AllTeams)
                {
                  <tr>
                      <td>@item.PlayerOnTeam.FirstName @item.PlayerOnTeam.LastName</td>
                      <td>@item.TeamOfPlayer.Location @item.TeamOfPlayer.TeamName</td>
                  </tr>
                }
            </table>
            <li>...all players, past and present, with the Manitoba Tiger-Cats</li>
            <table class="table table-striped col-10">
                <tr>
                  <th>Player</th>
                  <th>Team</th>
              </tr>
              @foreach (PlayerTeam item in ViewBag.two.AllPlayers)
                {
                  <tr>
                      <td>@item.PlayerOnTeam.FirstName @item.PlayerOnTeam.LastName</td>
                      <td>@item.TeamOfPlayer.Location @item.TeamOfPlayer.TeamName</td>
                  </tr>
                }
            </table>
            <li>...all players who were formerly (but aren't currently) with the Wichita Vikings</li>
            <table class="table table-striped col-10">
              <tr>
                <th>Player</th>
                <th>Past Team</th>
              </tr>
              @foreach (var item in ViewBag.three)
              {
                <tr>
                  <td>@item.PlayerOnTeam.FirstName @item.PlayerOnTeam.LastName</td>
                  <td>@item.TeamOfPlayer.Location @item.TeamOfPlayer.TeamName</td>
                </tr>
                  }
            </table>
            <li>...every team that Jacob Gray played for before he joined the Oregon Colts</li>
            <table class="table table-striped col-10">
              <tr>
                <th>Player</th>
                <th>Teams</th>
                <th>Current Team</th>
              </tr>
              <tr>
                @foreach (var item in ViewBag.four)
                {
                <td>@item.PlayerOnTeam.FirstName @item.PlayerOnTeam.LastName</td>
                <td>@item.TeamOfPlayer.Location @item.TeamOfPlayer.TeamName</td>
                <td>@item.PlayerOnTeam.CurrentTeam.Location @item.PlayerOnTeam.CurrentTeam.TeamName</td>
                }
              </tr>
            </table>
            <li>...everyone named "Levi" who has ever played in the Atlantic Federation of Amateur Baseball Players</li>
            <table class="table table-striped col-5">
              <tr>
                  <th>Player</th>
                  <th>Team</th>
                  <th>League</th>
              </tr>
              @foreach (PlayerTeam item in ViewBag.five)
                {
                    <tr>
                        <td>@item.PlayerOnTeam.FirstName @item.PlayerOnTeam.LastName</td>
                        <td>@item.TeamOfPlayer.Location @item.TeamOfPlayer.TeamName</td>
                        <td>@item.TeamOfPlayer.CurrLeague.Name</td>
                    </tr>
                }
            </table>
            <li>...all teams that have had 12 or more players, past and present.</li>
            <table class="table table-striped col-5">
              <tr>
                <th>Team</th>
                <th>Player Count</th>
              </tr>
              @* <tr>
                @* @foreach (var item in ViewBag.six)
                {
                <td>@item.Location @item.TeamName</td>
                <td>@(item.CurrentPlayers.Count+item.AllPlayers.Count)</td> *@
                }
              </tr> *@
            </table>
            <li>...all players, sorted by the number of teams they've played for</li>
            <table class="table table-striped col-5">
              <tr>
                  <th>Player</th>
                  <th>Team Count</th>
              </tr>
              @foreach (Player item in ViewBag.six)
                {
                    <tr>
                        <td>@item.FirstName @item.LastName</td>
                        <td>@item.AllTeams.Count </td>
                    </tr>
                }
            </table>
        </ol>
    </div>
</div>  